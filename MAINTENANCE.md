# מדריך תחזוקה ועדכון - סימולטור סלולר 2026

קובץ זה נועד לסייע בעדכון האפליקציה בעתיד במידה ויחולו שינויים במחירי המכשירים, מדרגי הזכאות או הנחיות המכרז.

## 📍 איפה נמצא כל דבר?

### 1. עדכון נתונים (מחירים, מכשירים, דרגות)
כל הנתונים הסטטיים של האפליקציה מרוכזים בתחילת הקובץ: `src/App.jsx`.
חפשו את המשתנים הבאים:
- `TIERS_DATA`: הגדרת מדרגי הזכאות וגובה ההשתתפות הממשלתית לכל דרגה.
- `DEVICES_CATALOG`: רשימת המכשירים, החלוקה לקטגוריות והמחיר הכולל לצרכן (כולל מע"מ).
- `MAINTENANCE_DATA`: טבלת ההשתתפות העצמית בנזקים/אובדן.
- `FAQ_DATA`: השאלות והתשובות שמופיעות בדף השו"ת.

### 2. עיצוב ומראה (UI)
- **Tailwind CSS**: רוב העיצוב מוטמע ישירות בתוך ה-JSX בעזרת `className`.
- **Custom CSS**: הגדרות גופנים (Heebo), רקעים מיוחדים וטיפוגרפיה נמצאים ב-`src/App.css`.
- **Icons**: משתמשים בספריית `lucide-react`.

### 3. מסמכי מקור
תיקיית `docs/` מכילה את קובץ ה-PDF הרשמי שעליו מתבססת האפליקציה. אם יוצאת גרסה חדשה למכרז, מומלץ לעדכן שם את הקובץ.

---

## 💻 פקודות עבודה

### הרצה מקומית לצורך בדיקות
```bash
npm run dev
```
(בדרך כלל האפליקציה תעלה בכתובת http://localhost:5173 או 5174)

### שמירה ועדכון ב-GitHub
לאחר שביצעתם שינויים בקוד, הריצו את הפקודות הבאות בטרמינל:
```bash
git add .
git commit -m "הסבר קצר על השינוי שבוצע"
git push origin main
```

---
**הערה:** האפליקציה נבנתה בצורה מודולרית. אם מוסיפים מכשיר חדש ל-`DEVICES_CATALOG`, הוא יופיע אוטומטית במחשבון תחת הקטגוריה המתאימה.
